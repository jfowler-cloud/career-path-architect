{
  "name": "@career-path/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn career_path.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  }
}
